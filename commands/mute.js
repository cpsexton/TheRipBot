// const Discord = require('discord.js');
// const client = new Discord.Client();
// client.commands = new Discord.Collection();
// const role = new Discord.Role();

// module.exports = {
//     name: "mute",
//     description: " takes in a username and mutes them for a certain time. ADMIN ONLY ",
//     async execute(message, args) {
//         let taggedUser = message.mentions.members.first();

//         if (!taggedUser) {
//             return message.channel.send(`${message.author}, that user cannot be found.\n    *(ex: $mute @TheRipBot 5)*`);
//         };
//         let timeoutAmount;
//         if ( /^\d+$/.test(args[3])) {
//             timeoutAmount = parseInt( rgs[3])
//         };
//         if (!timeoutAmount || timeoutAmout < 1 || timeoutAmount > 10) timeoutAmout = "Please provide a time. Max 10 minutes.";
//         6 
        


// $mute @Ripoff 10
    

// }
// };
// const time = ' ';
// time.push
